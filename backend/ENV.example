## Copy this file to .env and fill only what you need

# Required (set only what you use)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=...
# GOOGLE_API_KEY=...

# Default provider when request.provider is omitted
LLM_DEFAULT_PROVIDER=openai

# Optional environment label
ENVIRONMENT=development

# Optional per-model defaults (JSON string)
# Example: set temperature/max_tokens for specific models
# LLM_MODEL_DEFAULTS_JSON='{"openai":{"gpt-4o":{"temperature":0.7,"max_tokens":1024}},"gemini":{"gemini-2.5-pro":{"temperature":0.6}}}'
